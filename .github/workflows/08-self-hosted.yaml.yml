name: self hosted

on:
  workflow_dispatch:

jobs:
  self-hosted:
    runs-on:
      - self-hosted
      - Linux
      - X64

    steps:
      - name: Greet the world
        run: echo "Hello from self hosted runner!"